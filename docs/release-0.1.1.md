# 0.1.1 发布准备

检查日期：2026-09-17。候选版本：`0.1.1`。
已发布基线：NuGet `0.1.0`（标签 `v0.1.0`，2026-07-24）。
发布提交包含 `5049e14` 之后的应用名称解耦、`VersionPrefix` 调整，以及本轮真实数据库验收。

## 相对 v0.1.0 的说明

- EF Core 依赖下限由 10.0.10 提升至 10.0.12，SDK 基线为 10.0.401；保留当前依赖范围和锁文件。
- 补充查询翻译、JSON 比较和事务隔离级别边界的回归覆盖与中文兼容性说明。
- 将应用兼容性测试中的具体框架名称改为通用名称，移除特定应用文档，调整许可证署名。
- 提供程序实现源码相对 v0.1.0 无变化；本次不声明新增运行时能力或完整 EF 关系数据库一致性。

## 最终验证

- 使用 `/Users/wanghongyi/.dotnet/dotnet` 的 SDK 10.0.401。
- 锁定还原通过，未修改依赖锁文件。
- Release 构建通过：0 警告、0 错误。
- 单元测试：188 通过、0 失败、0 跳过。
- 参考服务器功能测试：44 通过、0 失败、0 跳过。
- 提供程序自有关系数据库冒烟测试：4 通过、0 失败、0 跳过。
- `dotnet format --verify-no-changes --no-restore` 通过。
- `git diff --check` 通过。
- 项目 `VersionPrefix` 为 `0.1.1`；标签工作流仍以 `PackageVersion` 覆盖包版本。
- 本地检查包仅用于核对 nuspec 与内容，正式包由 `v0.1.1` 标签工作流构建并推送到 NuGet.org。

连接字符串未写入仓库、未进入文档、未出现在测试输出中。
